# Base URL of the API Gateway (routes /api/auth, /api/restaurants,
# /api/catalogue, /api/orders to the backend services). Do not point this
# directly at an individual service's port.
VITE_API_BASE_URL=http://localhost:8080
